{
    "name": "ims-logistics",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "composer install --no-dev --optimize-autoloader && php artisan config:cache && vite build",
        "dev": "vite",
        "start": "php artisan migrate --force && php artisan serve --host 0.0.0.0 --port 10000"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.14.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
